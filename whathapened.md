# What have happened ?
## 1. VLM (Qwen) had trained only on vision adapter (Its own vision) with seismic images and labels (trained by project it into classes)

    1. Main Training Architecture
    
    
    Stage A: train_decoder_with_label()
    
    
    raw seismic image
    
        |
    
        v
    
    Unsloth Qwen-VL processor
    
        |
    
        v
    
    Qwen-VL vision tower
    
        |
    
        v
    
    trainable vision LoRA
    
        |
    
        v
    
    frozen language decoder
    
        |
    
        v
    
    label text target
    
        |
    
        v
    
    SFT loss
    
    
    Purpose:
    
    
    teach vision side what seismic classes look like
    
    
    Output:
    
    
    outputs/vision_llm_trained/final

## 2. After that, VLM had learnt the meaningful seismic's shape and be able to produce same semantic as label into vision's latent space.
## 3. Knowledge Distillation only on text decoder of VLM (teacher-student models) to make decoder understand each label descriptively.
## 4. Alignment happened on both encoder and decoder through loss functions (happened on share latent spaces) which determines that grammars generated by Decoder will be related to topics (label) while allow model to add its own explanation.
    Stage B: train_decoder_without_label()
    
      raw seismic image
            |
            v
      Qwen-VL + merged trained vision adapter
            |
            +-----------------------------+
            |                             |
            v                             v
      image-token hidden states     text-token hidden states
            |                             |
            v                             v
      frozen vision projector       trainable decoder projector
            |                             |
            v                             v
      vision latent                 decoder/token latents
            |                             |
            +----------- losses ----------+
    
      Losses:
    
      InfoNCE
      ProxyAnchor
      NormalizedSoftmax
      coverage
      pooled cosine alignment
      token-level latent alignment
    
      Purpose:
    
      align decoder representation with seismic-aware visual representation
    
      Output:
    
      outputs/vision_llm_trained_custom/final


# What should have changed?
## 1. VLM's vision encoder normally is from Qwen model without understanding the textures of images (gray scale!), since it's trained on RGB before ,so it is only understanding pixel relation (shaped) and that make it clear to attach seismic level encoder (SeisBert's knowledge) as auxiliary encoder during training
## 2. Knowledge Distillation of decoder should be used in the right way (using logits of teacher model)
## 3. Currently, Model did not understand what to do with seismic's image nor have any ways to evaluate on its own explanation
