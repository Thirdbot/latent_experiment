# transfer learning from geoscience model to student ,so the architecture doesn't need to change

#steps: 1.train_with_label(vision) 2.transfer_learning(text) 3.train_with_no_label(vision+text alignment)